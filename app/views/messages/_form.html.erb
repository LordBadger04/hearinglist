<%= simple_form_for [chat, message] do |f| %>
  <div class="chat-form">
    <%= f.input :content,
                label: false,
                required: true,
                placeholder: "Ask about a song, artist or live cover..." %>

    <%= f.button :submit, "Ask AI", class: "btn btn-dark" %>
  </div>
<% end %>
